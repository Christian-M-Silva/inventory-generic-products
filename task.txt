x Inicializar o sistema com ts
x Crie e configure o tsconfig.json com:
    x "strict": true
    x "target": "ES2020"
    x "module": "CommonJS"
x Criar um type chamado category, para representar uma categoria de produtos, que só pode ser "electronics", "clothing" ou "food"
x Criar interface com nome Product com essas props:
    x id: número
    x name: string
    x price: número
    x inStock: boolean
    x category: o type que eu criei chamado category
x Criar um type generico InventoryItem<T> que vai representar um objeto que é um item de inventário que possui:
    x uma propriedade data do tipo genérico T
    x uma propriedade quantity: número
x Criar uma função genérica que
    x Receba por param uma lista de InventoryItem<Product>
    x Soma todos os quantity (que é o produto em estoque) recebidos e armazena esse valor
    x Retorne o total de produtos em estoque (quantity de todos os itens somados).
- Criar uma chamada para função passando o product como generic
